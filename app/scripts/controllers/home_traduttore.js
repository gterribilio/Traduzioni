'use strict';

var home = angular.module('HomeCtrlTraduttoreModule', []);

home.controller('HomeTraduttoreCtrl',  ['$scope', '$rootScope', '$window', 'services', '$location', 'customFactory',
	function ($scope, $rootScope, $window, services, $location, customFactory) {  
	}]);